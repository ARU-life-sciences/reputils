// NOT YET FUNCTIONAL. TODO.

pub mod ttc {
    pub fn ttc(matches: &clap::ArgMatches) {
        // for a multiple alignment fasta, which contains the consensus
        // trim whole alignment to the suspected TE sequence.
        // how to do this??
        // longest contiguous stretch of non dashes?
        // or combine long contiguous stretches, separated by gaps
        // TODO.
    }
}
